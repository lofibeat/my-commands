```bash

📦 Пакеты и управление ПО

## Debian / Ubuntu (apt)

apt update                      # обновить список пакетов
apt upgrade                     # обновить все пакеты
apt full-upgrade                # обновить с удалением конфликтующих пакетов
apt install package             # установить пакет
apt install package1 package2   # установить несколько пакетов
apt remove package              # удалить пакет (сохраняет конфиг)
apt purge package               # удалить пакет и его конфигурацию
apt autoremove                  # удалить неиспользуемые зависимости
apt search keyword              # поиск пакета по названию или описанию
apt list --installed            # список установленных пакетов
apt list --upgradable           # список пакетов с доступными обновлениями
apt show package                # информация о пакете
apt-cache search keyword        # поиск в кэше (быстрее, чем apt search)
apt-cache show package          # показать детали пакета из кэша
dpkg -l | grep pattern          # список установленных пакетов (фильтр по паттерну)
dpkg -l                         # полный список установленных пакетов
dpkg -S /path/to/file           # найти какой пакет содержит файл
apt-file list package           # показать все файлы в пакете (требует apt-file install)
apt-file search filename        # найти пакет по имени файла (требует apt-file install)

## RHEL / CentOS / Fedora (yum / dnf)

yum update                      # обновить все пакеты (RHEL/CentOS 7 и старше)
yum install package             # установить пакет
yum remove package              # удалить пакет
yum search keyword              # поиск пакета
yum info package                # информация о пакете
yum list installed              # список установленных пакетов
yum list available              # список доступных пакетов
yum groupinstall "Group Name"   # установить группу пакетов
yum clean all                   # очистить кэш yum

dnf update                      # обновить все пакеты (Fedora, newer CentOS/RHEL)
dnf install package             # установить пакет
dnf remove package              # удалить пакет
dnf search keyword              # поиск пакета
dnf info package                # информация о пакете
dnf list installed              # список установленных пакетов
dnf groupinstall "Group Name"   # установить группу пакетов
dnf autoremove                  # удалить неиспользуемые зависимости

rpm -qa | grep pattern          # список установленных пакетов (фильтр)
rpm -qi package                 # информация об установленном пакете
rpm -ql package                 # список файлов в пакете
rpm -qf /path/to/file           # найти пакет, содержащий файл
rpm -ivh package.rpm            # установить .rpm файл напрямую
rpm -evh package                # удалить пакет

## Arch Linux (pacman)

pacman -Syu                     # обновить всю систему (S=sync, y=refresh, u=upgrade)
pacman -S package               # установить пакет
pacman -R package               # удалить пакет
pacman -Rs package              # удалить пакет и его зависимости
pacman -Ss keyword              # поиск пакета
pacman -Si package              # информация о пакете (из репозитория)
pacman -Qi package              # информация об установленном пакете
pacman -Ql package              # список файлов в пакете
pacman -Qo /path/to/file        # найти пакет, содержащий файл
pacman -Qdt                     # найти осиротелые зависимости
pacman -Sc                      # очистить кэш пакетов (оставляет последние версии)
pacman -Scc                     # очистить весь кэш пакетов

yay -S package                  # установить пакет через AUR (если установлен yay)
yay -Ss keyword                 # поиск пакета в AUR

## Универсальные менеджеры

### Snap (работает на всех дистрибутивах)

snap install package            # установить пакет из Snap Store
snap remove package             # удалить пакет
snap refresh package            # обновить пакет
snap list                       # список установленных snap пакетов
snap search keyword             # поиск пакета в Snap Store

### Flatpak (работает на всех дистрибутивах)

flatpak install flathub package # установить пакет из Flathub
flatpak remove package          # удалить пакет
flatpak update                  # обновить все пакеты
flatpak list --app              # список установленных приложений
flatpak search keyword          # поиск пакета
flatpak run org.example.app     # запустить flatpak приложение

## Управление зависимостями и конфликтами

`apt install -f`                  # исправить сломанные зависимости (Debian/Ubuntu)
apt --fix-broken install        # альтернативный способ исправить зависимости
apt install package1=version    # установить конкретную версию пакета

yum install --skip-broken package  # установить пакет, пропуская конфликты (RHEL/CentOS)
yum deplist package             # показать зависимости пакета

## Информация о системе пакетов

apt-cache depends package       # показать зависимости пакета (Debian/Ubuntu)
apt-cache rdepends package      # показать пакеты, зависящие от этого пакета
yum deplist package             # показать зависимости пакета (RHEL/CentOS)
yum repolist                    # список подключённых репозиториев

## Работа с локальными пакетами

dpkg -i package.deb             # установить локальный .deb пакет
rpm -ivh package.rpm            # установить локальный .rpm пакет
tar -xzf package.tar.gz && cd package && ./configure && make && sudo make install  # установка из исходников

## Примеры часто используемых команд

# Обновить систему полностью (Debian/Ubuntu)
sudo apt update && sudo apt upgrade && sudo apt autoremove

# Установить несколько пакетов с поиском (Debian/Ubuntu)
apt search nginx | grep "^nginx"
sudo apt install nginx

# Откатить пакет на старую версию (Debian/Ubuntu)
apt install package=old-version

# Найти какой пакет содержит команду (Debian/Ubuntu)
apt-file update
apt-file search bin/command-name

# Очистить кэш и неиспользуемые пакеты (Debian/Ubuntu)
sudo apt clean && sudo apt autoclean && sudo apt autoremove

# Список пакетов с размерами (Debian/Ubuntu)
dpkg-query -W -f='${Installed-Size}\t${Package}\n' | sort -rn | head -20