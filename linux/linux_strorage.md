```bash

💾 Работа с дисками и хранилищем

## Базовая информация о дисках и разделах

lsblk                           # список блочных устройств в древовидном формате
lsblk -a                        # показать все устройства (включая loop)
lsblk -o NAME,SIZE,TYPE,MOUNTPOINT  # показать конкретные колонки
fdisk -l                        # показать все разделы и диски (требует root)
parted -l                       # показать диски и разделы (modern way)
blkid                           # показать UUID и тип файловой системы
blkid /dev/sda1                 # показать информацию о конкретном разделе
lsscsi                          # список SCSI устройств
dmidecode -t memory             # информация о памяти и оборудовании

## Использование дискового пространства

df -h                           # использование диска по файловым системам (human-readable)
df -i                           # использование inodes
df -T                           # показать тип файловой системы
du -sh dir                      # размер директории
du -sh dir/*                    # размер каждого файла/папки в директории
du -sh -- * | sort -rh | head -10  # топ 10 самых больших папок
du -ah dir                      # размер каждого файла (рекурсивно)
ncdu /path                      # интерактивный анализ дискового пространства (если установлен)

## Работа с разделами (fdisk / parted)

### fdisk (для MBR разделов)

fdisk /dev/sda                  # интерактивное управление разделами диска
fdisk -l /dev/sda               # показать разделы конкретного диска
fdisk -u=cylinders /dev/sda     # показать в цилиндрах вместо секторов

### parted (для GPT и MBR)

parted /dev/sda                 # интерактивное управление разделами (modern)
parted -l                       # показать все диски и разделы
parted /dev/sda print           # показать разделы диска /dev/sda
parted /dev/sda unit s print    # показать в секторах
parted /dev/sda mkpart primary ext4 1MiB 10GiB  # создать раздел
parted /dev/sda rm 1            # удалить раздел номер 1
parted /dev/sda resizepart 1 20GiB  # изменить размер раздела

## Форматирование и создание файловых систем

mkfs.ext4 /dev/sda1             # создать ext4 файловую систему
mkfs.ext3 /dev/sda1             # создать ext3 файловую систему
mkfs.btrfs /dev/sda1            # создать Btrfs файловую систему
mkfs.xfs /dev/sda1              # создать XFS файловую систему
mkfs.vfat /dev/sda1             # создать FAT32 файловую систему
mkfs.ntfs /dev/sda1             # создать NTFS файловую систему
mkswap /dev/sda1                # создать swap раздел

## Монтирование и размонтирование

mount                           # показать все примонтированные файловые системы
mount -t ext4                   # показать примонтированные ext4 FS
mount /dev/sda1 /mnt/point      # примонтировать раздел
mount -o ro /dev/sda1 /mnt      # примонтировать в режиме только для чтения
mount -o remount,rw /mnt        # переиндексировать раздел в режим чтения-записи
mount --bind /source /target    # bind mount (подмонтировать папку)
umount /mnt/point               # отмонтировать
umount -l /mnt/point            # lazy unmount (отмонтировать когда освободится)
mount -a                        # примонтировать все из /etc/fstab
findmnt                         # показать дерево примонтированных FS
findmnt /dev/sda1               # показать где примонтирован раздел

## Проверка и ремонт файловых систем

fsck                            # проверить все файловые системы из /etc/fstab
fsck /dev/sda1                  # проверить конкретный раздел
fsck.ext4 -n /dev/sda1          # проверить в режиме read-only (без изменений)
fsck.ext4 -y /dev/sda1          # автоматически исправлять ошибки
e2fsck /dev/sda1                # проверить ext2/3/4 (альтернатива fsck)
e2fsck -p /dev/sda1             # автоматический режим
tune2fs -l /dev/sda1            # показать параметры ext FS
dumpe2fs /dev/sda1              # детальная информация о ext FS
xfs_repair /dev/sda1            # ремонт XFS файловой системы
btrfs filesystem show           # информация о Btrfs FS

## LVM (Logical Volume Manager)

### Работа с Physical Volumes (PV)

pvcreate /dev/sda1              # создать физический том
pvdisplay                       # показать все физические тома
pvdisplay /dev/sda1             # информация о конкретном PV
pvs                             # краткий список PV
pvrename /dev/sda1 /dev/sdb1    # переименовать PV
pvremove /dev/sda1              # удалить PV (требует удаления VG перед этим)

### Работа с Volume Groups (VG)

vgcreate vg-name /dev/sda1      # создать группу томов
vgcreate vg-name /dev/sda1 /dev/sdb1  # создать VG из нескольких PV
vgdisplay                       # показать все группы томов
vgdisplay vg-name               # информация о конкретной VG
vgs                             # краткий список VG
vgextend vg-name /dev/sdc1      # добавить PV к существующей VG
vgreduce vg-name /dev/sdc1      # удалить PV из VG
vgremove vg-name                # удалить группу томов
vgrename old-name new-name      # переименовать VG

### Работа с Logical Volumes (LV)

lvcreate -L 10G -n lv-name vg-name  # создать логический том размером 10GB
lvcreate -l 50%VG -n lv-name vg-name  # создать LV размером 50% от VG
lvdisplay                       # показать все логические тома
lvdisplay /dev/vg-name/lv-name  # информация о конкретном LV
lvs                             # краткий список LV
lvextend -L +5G /dev/vg-name/lv-name  # увеличить LV на 5GB
lvextend -l +100%FREE /dev/vg-name/lv-name  # увеличить LV на всё свободное место
lvreduce -L 5G /dev/vg-name/lv-name  # уменьшить LV на 5GB (опасно!)
lvremove /dev/vg-name/lv-name   # удалить логический том
lvrename vg-name old-lv new-lv  # переименовать LV

### Изменение размера файловой системы после изменения LV

resize2fs /dev/vg-name/lv-name  # изменить размер ext2/3/4 FS
xfs_growfs /mnt/point           # увеличить размер XFS FS (требует примонтирования)
btrfs filesystem resize +5G /mnt/point  # изменить размер Btrfs

## Работа с RAID

mdadm --create /dev/md0 --level=1 --raid-devices=2 /dev/sda1 /dev/sdb1  # создать RAID 1
mdadm --detail /dev/md0         # показать информацию о RAID
mdadm --examine /dev/sda1       # показать метаданные RAID
mdadm --manage /dev/md0 --add /dev/sdc1  # добавить диск к RAID
mdadm --manage /dev/md0 --remove /dev/sdb1  # удалить диск из RAID
mdadm --manage /dev/md0 --fail /dev/sdb1  # отметить диск как неисправный
cat /proc/mdstat                # показать статус RAID

## Шифрование (LUKS)

cryptsetup luksFormat /dev/sda1 # зашифровать раздел
cryptsetup luksOpen /dev/sda1 encrypted-name  # открыть зашифрованный раздел
cryptsetup luksClose encrypted-name  # закрыть зашифрованный раздел
cryptsetup luksDump /dev/sda1   # показать информацию о LUKS контейнере

## Работа с образами дисков

dd if=/dev/sda of=disk-image.img  # создать образ диска (полный)
dd if=/dev/sda of=disk-image.img bs=4M status=progress  # с прогрессом
dd if=disk-image.img of=/dev/sda bs=4M  # восстановить диск из образа
rsync -av --progress /source/ /dest/  # быстрое копирование директорий
tar -czvf backup.tar.gz /data/  # создать сжатый архив (резервная копия)

## Информация о дисках и SMART

smartctl -a /dev/sda            # показать SMART данные диска
smartctl -t short /dev/sda      # запустить быструю проверку SMART
smartctl -t long /dev/sda       # запустить полную проверку SMART
smartctl -l selftest /dev/sda   # показать результаты SMART тестов

## /etc/fstab (конфигурация автомонтирования)

# Пример строки в /etc/fstab:
# /dev/sda1  /mount/point  ext4  defaults,errors=remount-ro  0  2

cat /etc/fstab                  # показать конфиг автомонтирования
nano /etc/fstab                 # редактировать конфиг
mount -a                        # проверить и примонтировать все из fstab