```bash

🌐 Сеть и сетевой мониторинг (расширенный)

## Информация об интерфейсах

ip addr show                    # показать IP-адреса и состояние интерфейсов
ip addr show eth0               # информация о конкретном интерфейсе
ip link show                    # показать состояние всех интерфейсов (уровень 2)
ip -s link show                 # показать статистику интерфейсов
ifconfig                        # показать конфигурацию интерфейсов (legacy)
ifconfig -a                     # показать все интерфейсы включая неактивные
ifconfig eth0 up                # включить интерфейс
ifconfig eth0 down              # отключить интерфейс
ethtool eth0                    # информация о интерфейсе (скорость, режим)
ethtool -S eth0                 # статистика интерфейса
nmcli device show               # информация об устройствах (NetworkManager)
nmcli device status             # статус всех устройств

## Конфигурация IP адресов

ip addr add 192.168.1.10/24 dev eth0  # добавить IP адрес на интерфейс
ip addr del 192.168.1.10/24 dev eth0  # удалить IP адрес с интерфейса
ip link set eth0 up             # включить интерфейс
ip link set eth0 down           # отключить интерфейс
ip link set eth0 mtu 1500       # изменить MTU интерфейса

nmcli connection add type ethernet con-name "Home" ifname eth0  # создать соединение (NM)
nmcli connection up "Home"      # включить соединение (NM)
nmcli connection down "Home"    # отключить соединение (NM)

## Маршрутизация

ip route                        # показать таблицу маршрутизации
ip route show                   # альтернатива ip route
ip route add 192.168.2.0/24 via 192.168.1.1  # добавить маршрут
ip route del 192.168.2.0/24     # удалить маршрут
ip route add default via 192.168.1.1  # добавить маршрут по умолчанию
route -n                        # показать маршруты в числовом формате (legacy)
route add default gw 192.168.1.1  # добавить шлюз (legacy)
traceroute host                 # трассировка маршрута до хоста
traceroute -m 15 host           # трассировка с максимум 15 прыжков
mtr host                        # интерактивная трассировка (ping + traceroute)
mtr -r -c 10 host               # мониторинг хоста 10 раз и вывод результата

## ARP (Address Resolution Protocol)

arp -a                          # показать ARP таблицу
arp -n                          # показать ARP таблицу в числовом формате
ip neigh show                   # показать ARP таблицу (современный способ)
ip neigh add 192.168.1.20 lladdr aa:bb:cc:dd:ee:ff dev eth0  # добавить ARP запись
ip neigh del 192.168.1.20 dev eth0  # удалить ARP запись
arping -c 3 192.168.1.1         # ARP пинг шлюза/интерфейса
arp-scan -l                     # сканировать локальную сеть (требует установки)

## DNS и разрешение имён

nslookup domain                 # показать IP по доменному имени (legacy)
nslookup domain 8.8.8.8         # запрос к конкретному DNS серверу
dig domain                      # подробный DNS запрос
dig domain +short               # короткий ответ DNS
dig domain ANY                  # показать все DNS записи
dig domain MX                   # показать MX записи (почтовые серверы)
dig domain NS                   # показать NS записи (nameservers)
dig +trace domain               # трассировка DNS запроса
dig -x 8.8.8.8                  # обратный DNS запрос (IP -> имя)
host domain                     # простой DNS запрос
host -a domain                  # подробный DNS запрос
getent hosts domain             # показать запись из /etc/hosts или DNS
cat /etc/resolv.conf            # показать конфигурацию DNS
cat /etc/hosts                  # показать локальную таблицу имён
systemd-resolve --status        # статус resolver (systemd-resolved)
resolvectl query domain         # DNS запрос через systemd-resolved

## Проверка соединения и доступности

ping host                       # проверить доступность хоста
ping -c 4 host                  # отправить 4 пинга и выйти
ping -i 0.2 host                # пинг с интервалом 0.2 секунды
ping -s 1500 host               # пинг с размером пакета 1500 байт
ping -f host                    # flood ping (быстрый, требует root)
fping -g 192.168.1.0/24         # проверить доступность сети (если установлен)
nc -zv host 22                  # netcat: проверить открыт ли порт 22
nc -l -p 1234                   # netcat: создать listener на порту 1234
timeout 5 bash -c 'cat < /dev/null > /dev/tcp/host/port'  # проверить порт

## Просмотр портов и соединений

ss -tulwn                       # показать слушающие порты и соединения (современный)
ss -tulwn | grep LISTEN         # только слушающие сокеты
ss -tulpn                       # с информацией о процессах (требует root)
ss -s                           # сводная статистика сокетов
ss -ta                          # показать все TCP соединения
ss -ua                          # показать все UDP соединения
ss -x                           # показать Unix сокеты
netstat -tulpn                  # показать слушающие порты и процессы (legacy)
netstat -s                      # статистика по протоколам
netstat -an | grep ESTABLISHED  # активные соединения
lsof -i                         # список всех сетевых сокетов
lsof -i :80                     # какой процесс использует порт 80
lsof -i TCP:22                  # TCP соединения на порту 22
lsof -iTCP -sTCP:ESTABLISHED    # все установленные TCP соединения
fuser 80/tcp                    # найти процесс, использующий порт 80

## Мониторинг сетевого трафика

tcpdump                         # сниффер пакетов (требует root)
tcpdump -i eth0                 # снифф на конкретном интерфейсе
tcpdump -n -i eth0 'tcp port 80'  # снифф HTTP трафика
tcpdump -w capture.pcap         # сохранить в файл
tcpdump -r capture.pcap         # прочитать из файла
tcpdump -A -i eth0 'tcp port 80'  # показать ASCII данные пакетов
iftop -i eth0                   # интерактивный мониторинг трафика по соединениям
iftop -n                        # без DNS разрешения (быстрее)
nethogs                         # мониторинг сети по процессам (требует root)
nethogs -d 1                    # обновлять каждую 1 секунду
vnstat                          # долговременная статистика трафика
vnstat -h                       # часовая статистика
vnstat -d                       # дневная статистика
vnstat -m                       # месячная статистика
bmon                            # графический мониторинг (если установлен)

## HTTP и веб запросы

curl https://example.com        # сделать GET запрос
curl -I https://example.com     # получить только заголовки
curl -X POST -d "data" https://example.com  # POST запрос с данными
curl -L https://example.com     # следовать редиректам
curl -u user:pass https://example.com  # с аутентификацией
curl -H "Custom-Header: value" https://example.com  # с кастомным заголовком
curl -v https://example.com     # verbose (показать запрос и ответ)
curl -s https://example.com | jq  # тихий режим и парсинг JSON
wget https://example.com        # скачать файл
wget --spider https://example.com  # проверить доступность без скачивания
wget -r https://example.com     # рекурсивно скачать сайт
curl --resolve example.com:443:1.2.3.4 https://example.com  # с принудительным IP

## SSH и удалённый доступ

ssh user@host                   # подключиться по SSH
ssh -v user@host                # verbose режим (показать детали)
ssh -p 2222 user@host           # нестандартный порт
ssh -i /path/to/key user@host   # использовать конкретный ключ
ssh -N -L 8080:localhost:80 user@host  # локальный port forwarding
ssh -N -R 8080:localhost:80 user@host  # удалённый port forwarding
ssh -D 1080 user@host           # SOCKS proxy через SSH
scp file user@host:/remote/path  # скопировать файл на удалённый хост
scp user@host:/remote/file .    # скопировать файл с удалённого хоста
scp -r dir/ user@host:/remote/  # рекурсивное копирование
rsync -avz src/ user@host:/dst/ # синхронизация с удалённым хостом
rsync -av --delete src/ /dst/   # синхронизация с удалением файлов
sftp user@host                  # интерактивный SFTP клиент

ssh-keygen -t ed25519           # сгенерировать SSH ключ (современный)
ssh-keygen -t rsa -b 4096       # сгенерировать RSA ключ
ssh-keygen -l -f ~/.ssh/id_ed25519  # показать отпечаток ключа
ssh-keygen -p -f ~/.ssh/id_ed25519  # изменить пароль ключа
ssh-copy-id user@host           # скопировать публичный ключ на сервер
ssh-agent bash                  # запустить SSH агент
ssh-add ~/.ssh/id_ed25519       # добавить ключ в агент
ssh-add -l                      # список ключей в агенте

## Фаервол и правила

sudo iptables -L -n -v          # показать текущие правила iptables (IPv4)
sudo iptables -L -n -v | grep Chain  # только цепи
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT  # добавить правило
sudo iptables -D INPUT -p tcp --dport 22 -j ACCEPT  # удалить правило
sudo iptables -F                # очистить все правила
sudo iptables-save > /etc/iptables/rules.v4  # сохранить правила

sudo nft list ruleset           # показать nftables правила (современный)
sudo nft add rule inet filter input tcp dport 22 accept  # добавить правило

sudo ufw status                 # статус UFW
sudo ufw status verbose         # подробный статус UFW
sudo ufw enable                 # включить UFW
sudo ufw disable                # отключить UFW
sudo ufw allow 22/tcp           # разрешить SSH
sudo ufw deny 80/tcp            # запретить HTTP
sudo ufw allow from 192.168.1.0/24  # разрешить подсеть

firewall-cmd --state            # статус firewalld
firewall-cmd --list-all         # список всех правил firewalld
firewall-cmd --add-port=80/tcp  # добавить порт (временно)
firewall-cmd --permanent --add-port=80/tcp  # добавить порт (постоянно)

## Диагностика и отладка

whois domain                    # WHOIS информация о домене
whois IP                        # WHOIS информация об IP
curl -I https://example.com     # проверить доступность и заголовки
timeout 5 bash -c 'cat < /dev/null > /dev/tcp/host/port' && echo "Port is open"
mtr -r -c 100 host              # детальная трассировка с потерями пакетов
nmap host                       # сканирование портов (если установлен)
nmap -p 1-65535 host            # сканировать все порты
netcat -u -l -p 5353            # прослушивать UDP (для тестирования)

## Конфигурационные файлы

cat /etc/hostname                # показать имя хоста
cat /etc/resolv.conf             # DNS конфигурация
cat /etc/network/interfaces      # конфиг интерфейсов (Debian, legacy)
cat /etc/netplan/                # конфиги netplan (современный способ)
cat /etc/sysconfig/network-scripts/  # конфиги интерфейсов (RHEL/CentOS)
systemctl restart networking     # перезагрузить сетевой сервис
nmcli connection reload          # перезагрузить соединения (NetworkManager)