```bash

📦 tar — архивы и сжатие (Linux / macOS)

## Создание архива

tar -cvf archive.tar dir/       # создать архив (c=create, v=verbose, f=file)
tar -cvf archive.tar file1 file2  # упаковать указанные файлы
tar -czvf archive.tar.gz dir/   # архив + gzip (расширение .tar.gz или .tgz)
tar -cJvf archive.tar.xz dir/  # архив + xz (сильнее сжатие, дольше)
tar -cvf archive.tar --exclude='*.log' dir/  # исключить файлы по маске

# Опции: -c создать, -v подробно, -f файл архива
# Сжатие: -z gzip, -J xz, -j bzip2 (редко)

## Распаковка

tar -xvf archive.tar            # распаковать (x=extract)
tar -xzvf archive.tar.gz       # распаковать .tar.gz
tar -xJvf archive.tar.xz       # распаковать .tar.xz
tar -xvf archive.tar -C /path  # распаковать в указанный каталог
tar -xvf archive.tar file.txt  # извлечь только один файл из архива
tar -tvf archive.tar           # показать содержимое без распаковки (t=list)
tar -tzvf archive.tar.gz       # список содержимого .tar.gz

## Шпаргалка флагов

# Режим: -c создать, -x извлечь, -t список
# Опции: -v подробно, -f файл (всегда последний после -f)
# Сжатие: -z gzip (.gz), -J xz (.xz), -j bzip2 (.bz2)
# Прочие: -C каталог (сменить директорию), -P не убирать ведущий / (абсолютные пути)

## gzip / gunzip (одиночные файлы)

gzip file                      # сжать file -> file.gz (исходный удаляется)
gzip -k file                   # сжать, сохранить исходный (-k keep)
gzip -d file.gz                # распаковать
gunzip file.gz                 # то же что gzip -d
gzip -9 file                   # максимальное сжатие (по умолчанию -6)
zcat file.gz                   # вывести содержимое без распакованного файла

## zip / unzip

zip archive.zip file1 file2     # создать zip-архив
zip -r archive.zip dir/        # рекурсивно каталог
unzip archive.zip              # распаковать в текущий каталог
unzip archive.zip -d /path     # распаковать в указанный каталог
unzip -l archive.zip           # список содержимого без распаковки
unzip -o archive.zip          # перезаписать без запроса (-o overwrite)

## 7z (если установлен)

7z a archive.7z dir/           # создать архив
7z x archive.7z                 # распаковать с полными путями
7z l archive.7z                 # список содержимого
7z e archive.7z                # извлечь без структуры каталогов
```
