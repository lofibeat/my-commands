🔐 1. Пользователи и права

whoami                          # текущий пользователь
id                              # UID, GID, группы
users                           # кто залогинен

useradd user                    # добавить пользователя
passwd user                     # сменить пароль у пользователя
usermod -aG sudo user           # добавить пользователя в группу sudo
userdel -r user                 # удалить пользователя и его домашнюю папку

groups user                     # посмотреть в каких группах состоит пользователь
groupadd dev                    # добавить группу
usermod -aG dev user            # добавить пользователя в группу

🔑 2. Права доступа (chmod / chown)

ls -l                           # показать подробный список файлов и их прав
stat file                       # показать детальную информацию о файле и его правах

chmod 755 file                  # дать права 755 (rwx r-x r-x) на файл
chmod 644 file                  # дать права 644 (rw- r-- r--) на файл
chmod u+x script.sh             # дать владельцу право на выполнение (u+x)
chmod -R 755 dir/               # рекурсивно дать права на папку
chmod -R u+rwx,g+rx,o-rwx dir/  # рекурсивно установить права буквенной нотацией

chown user file                 # изменить владельца файла
chown user:group file           # изменить владельца и группу файла
chown -R user:group dir/        # рекурсивно изменить владельца папки

chgrp group file                # изменить группу файла
chgrp -R group dir/             # рекурсивно изменить группу папки

umask 0022                      # установить маску прав для новых файлов

📁 3. Файлы и директории

pwd                             # показать текущую директорию
ls -la                          # список со скрытыми файлами и правами
cd dir                          # перейти в директорию
mkdir dir                       # создать директорию
mkdir -p dir/subdir             # создать директорию рекурсивно
rm file                         # удалить файл
rm -r dir                       # удалить директорию рекурсивно
cp file newfile                 # копировать файл
cp -r dir newdir                # копировать директорию рекурсивно
mv file newfile                 # переместить/переименовать файл
find /path -name "*.txt"        # найти файлы по имени
find /path -type f -size +100M  # найти файлы по размеру
du -sh dir                      # размер директории
df -h                           # использование диска по файловым системам

⚙️ 4. Процессы и сервисы

ps aux                          # список всех процессов
ps aux | grep process           # найти процесс по имени
top                             # монитор процессов в реальном времени
htop                            # улучшенный монитор процессов (если установлен)
kill PID                        # завершить процесс (SIGTERM)
kill -9 PID                     # принудительное завершение процесса (SIGKILL)

systemctl status service        # статус сервиса
systemctl start service         # запустить сервис
systemctl stop service          # остановить сервис
systemctl restart service       # перезапустить сервис
systemctl enable service        # включить запуск при загрузке
systemctl disable service       # отключить запуск при загрузке
systemctl list-units --type=service  # список всех сервисов

📝 5. Логи и отладка

journalctl -u service -f        # логи сервиса в реальном времени (follow)
journalctl -b                   # логи с текущей загрузки
journalctl -b -n 50             # последние 50 строк логов с текущей загрузки
tail -f /var/log/syslog         # последние строки лога в реальном времени
tail -n 20 /var/log/file        # показать последние 20 строк лога
grep pattern /var/log/file      # поиск в логе
grep -i pattern /var/log/file   # поиск в логе (игнорировать регистр)

📦 6. Пакеты и управление ПО

apt update                      # обновить список пакетов (Debian/Ubuntu)
apt upgrade                     # обновить пакеты (Debian/Ubuntu)
apt install package             # установить пакет
apt remove package              # удалить пакет
apt search keyword              # поиск пакета
apt list --installed            # список установленных пакетов
dpkg -l | grep pattern          # список установленных пакетов (фильтр)
apt-cache show package          # информация о пакете

💾 7. Архивы и сжатие

tar -czvf archive.tar.gz dir/   # создать архив .tar.gz
tar -xzvf archive.tar.gz        # распаковать архив .tar.gz
tar -tzf archive.tar.gz         # показать содержимое архива без распаковки
zip -r archive.zip dir/         # создать .zip архив
unzip archive.zip               # распаковать .zip
gzip file                       # сжать файл в .gz
gunzip file.gz                  # распаковать .gz

🌐 8. Сеть и сетевой мониторинг

apt install net-tools           # установить инструменты (пример для Debian/Ubuntu)
ip addr show                    # показать IP-адреса и состояние интерфейсов
ifconfig -a                     # показать интерфейсы (legacy)
ping host                       # проверить доступность хоста
ss -tulwn                       # показать слушающие порты и активные соединения
netstat -tulpn                  # показывает какие процессы слушают порты
traceroute host                 # трассировка по хосту
nslookup domain                 # показать IP по доменному имени (legacy)
dig domain                      # подробный DNS-запрос (рекомендуется)
lsof -i :80                     # посмотреть какой процесс использует порт
nc -l 1234                      # netcat — открыть простейший TCP-листенер
nc host port                    # netcat — тест подключения к порту
iftop -i eth0                   # интерактивный монитор трафика по соединениям

curl url                        # сделать http-запрос по урлу
wget url                        # скачать по урлу

ssh user@host                   # подключиться по SSH
scp file user@host:/path/       # копировать файл по SSH
rsync -avz src/ user@host:/dst/ # синхронизация директорий по rsync+ssh
ssh-keygen -t ed25519           # сгенерировать SSH ключ
