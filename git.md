```bash

🔗 Git - Система контроля версий

## Начальная конфигурация

git config --global user.name "Your Name"  # установить имя пользователя
git config --global user.email "email@example.com"  # установить email
git config --list                          # показать все настройки
git config user.name                       # показать конкретную настройку
git config --global core.editor nano       # установить редактор по умолчанию
git config --global init.defaultBranch main  # установить ветку по умолчанию

## Создание и инициализация

git init                        # инициализировать репозиторий в текущей папке
git init project-name           # создать новую папку и инициализировать
git clone url                   # клонировать репозиторий
git clone url folder-name       # клонировать в конкретную папку
git clone --depth 1 url         # клон без истории (быстрее)
git clone --branch branch-name url  # клонировать конкретную ветку

## Статус и просмотр

git status                      # показать статус репозитория
git status -s                   # короткий формат статуса
git log                         # показать историю коммитов
git log --oneline               # одна строка на коммит
git log -n 5                    # показать последние 5 коммитов
git log --graph --oneline --all  # граф веток и коммитов
git log --author="name"         # коммиты конкретного автора
git log --since="2 weeks ago"   # коммиты за последние 2 недели
git log -p                      # показать изменения в каждом коммите
git log --stat                  # статистика изменений
git log --grep="pattern"        # поиск коммитов по сообщению
git diff                        # показать изменения в рабочей директории
git diff --cached               # показать изменения в staging area
git diff branch1 branch2        # различия между ветками
git diff HEAD~1 HEAD            # различия между последним и предпоследним коммитом
git show commit-hash            # показать конкретный коммит
git show commit-hash:file       # показать файл в конкретном коммите
git blame file                  # показать автора каждой строки файла
git reflog                      # история всех операций в репозитории

## Добавление и коммиты

git add file                    # добавить файл в staging area
git add .                       # добавить все изменённые файлы
git add -A                      # добавить все файлы (включая удаления)
git add -p                      # интерактивное добавление (по частям)
git add -u                      # добавить только отслеживаемые файлы
git commit -m "message"         # создать коммит с сообщением
git commit -am "message"        # добавить и создать коммит одновременно
git commit --amend              # изменить последний коммит
git commit --amend --no-edit    # добавить изменения в последний коммит без нового сообщения
git commit -m "message" --no-verify  # пропустить pre-commit hooks

## Отмена и возврат

git restore file                # отменить изменения в файле (не добавленные)
git checkout file               # альтернатива git restore
git reset file                  # убрать файл из staging area
git reset HEAD file             # альтернатива
git reset --soft HEAD~1         # отменить последний коммит (сохранить изменения)
git reset --mixed HEAD~1        # отменить последний коммит (убрать из staging)
git reset --hard HEAD~1         # полностью отменить последний коммит
git revert commit-hash          # создать новый коммит, отменяющий старый
git revert HEAD                 # отменить последний коммит новым коммитом
git clean -fd                   # удалить неотслеживаемые файлы и папки
git clean -fX                   # удалить файлы в .gitignore

## Ветки (Branches)

git branch                      # список локальных веток
git branch -a                   # список всех веток (локальных и удалённых)
git branch -v                   # ветки с информацией о последнем коммите
git branch branch-name          # создать новую ветку
git branch -d branch-name       # удалить ветку (если merged)
git branch -D branch-name       # принудительно удалить ветку
git branch -m old-name new-name  # переименовать ветку
git checkout branch-name        # переключиться на ветку
git checkout -b branch-name     # создать и переключиться на ветку
git switch branch-name          # переключиться на ветку (новый способ)
git switch -c branch-name       # создать и переключиться на ветку (новый способ)
git branch -u origin/branch-name  # установить upstream ветку

## Слияние (Merge)

git merge branch-name           # слить ветку в текущую
git merge --no-ff branch-name   # слить с созданием merge коммита
git merge --squash branch-name  # слить с объединением коммитов
git merge --abort               # отменить процесс слияния
git mergetool                   # открыть инструмент для разрешения конфликтов

## Rebase (изменение истории)

git rebase branch-name          # переписать историю текущей ветки на основе другой
git rebase -i HEAD~3            # интерактивный rebase последних 3 коммитов
git rebase --continue           # продолжить rebase после разрешения конфликтов
git rebase --abort              # отменить rebase
git rebase --onto branch-name commit-hash  # продвинутый rebase

## Тэги (Tags)

git tag                         # список тэгов
git tag tag-name                # создать лёгкий тэг
git tag -a tag-name -m "message"  # создать аннотированный тэг
git tag -d tag-name             # удалить локальный тэг
git show tag-name               # показать информацию о тэге
git push origin tag-name        # отправить тэг на удалённый репозиторий
git push origin --tags          # отправить все тэги

## Удалённые репозитории (Remote)

git remote                      # список удалённых репозиториев
git remote -v                   # список удалённых репозиториев с URL
git remote add name url         # добавить новый удалённый репозиторий
git remote remove name          # удалить удалённый репозиторий
git remote rename old-name new-name  # переименовать удалённый репозиторий
git remote set-url name new-url  # изменить URL удалённого репозитория
git remote show name            # информация об удалённом репозитории

## Push и Pull

git push                        # отправить коммиты на удалённый репозиторий
git push origin branch-name     # отправить конкретную ветку
git push -u origin branch-name  # отправить и установить upstream
git push origin --all           # отправить все ветки
git push origin --tags          # отправить все тэги
git push origin :branch-name    # удалить ветку на сервере
git pull                        # получить и слить изменения
git pull --rebase               # получить и переписать историю (вместо merge)
git fetch                       # получить изменения без слияния
git fetch origin                # получить с конкретного удалённого репозитория

## Stash (временное сохранение)

git stash                       # сохранить текущие изменения временно
git stash save "description"    # сохранить с описанием
git stash list                  # список сохранённых изменений
git stash pop                   # восстановить и удалить последние stash
git stash apply                 # восстановить последние stash (без удаления)
git stash apply stash@{n}       # восстановить конкретный stash
git stash drop                  # удалить последний stash
git stash drop stash@{n}        # удалить конкретный stash
git stash clear                 # удалить все stashes

## Cherry-pick (выбирание коммитов)

git cherry-pick commit-hash     # применить коммит из другой ветки
git cherry-pick commit1 commit2  # применить несколько коммитов
git cherry-pick branch-name~2..branch-name  # применить диапазон коммитов
git cherry-pick --continue      # продолжить после разрешения конфликтов
git cherry-pick --abort         # отменить cherry-pick

## Поиск и отладка

git grep "text"                 # поиск текста в файлах
git grep "text" branch-name     # поиск в конкретной ветке
git bisect start                # начать бинарный поиск ошибки
git bisect bad commit-hash      # отметить плохой коммит
git bisect good commit-hash     # отметить хороший коммит
git bisect reset                # завершить bisect
git log -S "text"               # поиск коммитов, добавивших/удаливших текст
git log -p -S "text"            # показать изменения с текстом

## Очистка и оптимизация

git gc                          # сборка мусора и оптимизация репозитория
git fsck                        # проверить целостность репозитория
git prune                       # удалить недостижимые объекты
git clean -fd                   # удалить неотслеживаемые файлы и папки
git clean -fdx                  # удалить неотслеживаемые файлы и папки (включая gitignore)

## Расширенные операции

git worktree list               # список рабочих деревьев
git worktree add path branch-name  # создать новое рабочее дерево
git submodule add url path      # добавить подмодуль
git submodule update --init --recursive  # инициализировать подмодули
git filter-branch --tree-filter 'rm -f file' HEAD  # удалить файл из истории (опасно!)
git format-patch -n             # создать patch файлы из последних n коммитов
git apply patch-file            # применить patch файл
git am patch-file               # применить patch с коммитом

## Конфигурация .gitignore

# Примеры игнорирования файлов
*.log                           # игнорировать все .log файлы
node_modules/                   # игнорировать папку
.env                            # игнорировать конкретный файл
!important.log                  # исключение (не игнорировать)
/build/                         # игнорировать в корне репозитория

## Полезные алиасы (добавить в конфиг)

git config --global alias.st status  # git st = git status
git config --global alias.co checkout  # git co = git checkout
git config --global alias.br branch  # git br = git branch
git config --global alias.ci commit  # git ci = git commit
git config --global alias.unstage 'restore --staged'  # отменить add
git config --global alias.last 'log -1 HEAD'  # показать последний коммит
git config --global alias.visual 'log --graph --oneline --all'  # красивая история