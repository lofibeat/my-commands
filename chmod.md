```bash

🔒 Права доступа chmod (Linux / macOS)

## Цифровой (восьмеричный) режим

# Формула: владелец(u) + группа(g) + остальные(o). Каждый разряд: r=4, w=2, x=1
# r = read (4), w = write (2), x = execute (1). Сумма = нужные права

chmod 644 file                  # файл: владелец rw-, группа r--, остальные r-- (типично для файлов)
chmod 755 file                  # владелец rwx, группа r-x, остальные r-x (типично для каталогов, скриптов)
chmod 600 file                  # только владелец rw- (секреты, ключи SSH)
chmod 640 file                  # владелец rw-, группа r-- (конфиги с доступом группе)
chmod 664 file                  # владелец и группа rw-, остальные r--
chmod 700 dir                   # каталог: только владелец rwx (закрытый каталог)
chmod 775 dir                   # владелец и группа rwx, остальные r-x (общая папка)
chmod 777 file                  # все rwx (небезопасно, только для теста)

## Символьный режим (кто операция права)

# Кто: u = user (владелец), g = group, o = other, a = all
# Операция: + добавить, - убрать, = установить ровно
# Права: r read, w write, x execute

chmod u+x file                  # владельцу добавить выполнение
chmod g-w file                  # у группы убрать запись
chmod o=r file                  # остальным установить только чтение
chmod a+x file                  # всем добавить выполнение (то же что chmod +x)
chmod u=rwx,g=rx,o= file        # владелец rwx, группа rx, остальные ничего
chmod go-w file                 # у группы и остальных убрать запись
chmod +x script.sh              # всем добавить выполнение (скрипт)
chmod -x file                   # у всех убрать выполнение

## Рекурсивно (каталоги)

chmod -R 755 dir/               # рекурсивно каталог и всё внутри
chmod -R u=rwX,g=rX,o=rX dir/   # каталогам X даёт x, файлам X — нет (удобно для дерева)
chmod -R a+rX dir/              # всем добавить r и X (X = x только для каталогов)

## Специальные биты (четвёртая цифра в восьмеричном режиме)

# 4 = setuid, 2 = setgid, 1 = sticky. Суммируются с обычными: 4755, 2755, 1777
chmod 4755 binary               # setuid: выполнять от владельца (например /usr/bin/passwd)
chmod 2755 dir                  # setgid: новые файлы наследуют группу каталога
chmod 1777 /tmp                 # sticky: удалить файл может только владелец (типично для /tmp)
chmod u+s file                  # setuid (символьно)
chmod g+s dir                   # setgid (символьно)
chmod +t dir                    # sticky (символьно)

## Просмотр текущих прав

ls -l file                      # одна строка: права, ссылки, владелец, группа, размер, имя
ls -la dir/                     # все файлы включая скрытые, с правами
stat -c '%a %n' file            # права в цифрах (Linux)
stat -f '%Lp %N' file           # права в цифрах (macOS)

## chown и chgrp (владелец и группа)

chown user file                 # сменить владельца
chown user:group file           # сменить владельца и группу
chown :group file               # сменить только группу
chown -R user:group dir/        # рекурсивно
chgrp group file                # сменить группу (альтернатива chown :group)
# Требуются права root (или capability) для смены владельца/группы

## umask (маска прав для новых файлов)

umask                           # показать текущую umask (например 022)
umask 022                       # новые файлы: 644, каталоги: 755 (типично)
umask 077                       # новые файлы: 600, каталоги: 700 (только владелец)
# Права = 666 - umask для файлов, 777 - umask для каталогов (без учёта +x)

## Частые значения (шпаргалка)

# 600  — ключи, секреты (только владелец rw)
# 644  — обычный файл (владелец rw, остальные r)
# 755  — каталог или исполняемый/скрипт (владелец rwx, остальные rx)
# 700  — закрытый каталог или ключ с выполнением (только владелец)
# 775  — общая папка с записью для группы
# 1777 — /tmp: все пишут, удалить может только владелец своего файла
# r=4  w=2  x=1  →  7=rwx  6=rw  5=rx  4=r  3=wx  2=w  1=x  0=—
